// Hook de autenticação — será implementado na Etapa 1.5
// Placeholder para não quebrar imports futuros

export function useAuth() {
  return {
    user: null,
    loading: false,
    signIn: async () => {},
    signUp: async () => {},
    signOut: async () => {},
  };
}